import { ComponentStyleConfig } from '@chakra-ui/react';

export default {
  parts: ['field'],
  variants: {
    outline: {
      field: {
        borderRadius: 0,
      },
    },
    filled: {
      field: {
        borderRadius: 0,
      },
    },
  },
} as ComponentStyleConfig;
