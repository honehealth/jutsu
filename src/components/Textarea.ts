import { ComponentStyleConfig } from '@chakra-ui/react';

export default {
  variants: {
    filled: {
      borderRadius: 0,
    },
    outline: {
      borderRadius: 0,
    },
  },
} as ComponentStyleConfig;
