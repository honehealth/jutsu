import { defineRecipe } from '@chakra-ui/react';

export default defineRecipe({
  variants: {
    variant: {
      filled: {
        borderRadius: 0,
      },
      outline: {
        borderRadius: 0,
      },
    },
  },
});
